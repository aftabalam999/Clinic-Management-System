# ────────────────────────────────────────────────
# FRONTEND ENVIRONMENT VARIABLES — copy to .env
# All variables MUST start with VITE_ to be exposed to the browser build
# ────────────────────────────────────────────────

# Your Render backend URL (no trailing slash, include /api)
VITE_BACKEND_URL=https://clinic-management-backend.onrender.com/api
